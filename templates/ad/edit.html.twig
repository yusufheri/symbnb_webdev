{% extends "base.html.twig" %}


{% block title %} Edition d'une annonce{% endblock %}

{% form_theme form 'ad/_collection.html.twig' %}

{% block body %}
    <div class="container">
        <h1 class="my-5">Modifier l'annonce : {{ ad.title }}</h1>
        {{ form_start(form) }}
            <div class="row">
                <div class="col">
                    <div class="alert alert-light">
                        <h2>Informations générales</h2>
                        <hr>
                        {{ form_row(form.title)}}
                        {{ form_row(form.city)}}
                        {{ form_row(form.price)}}
                        {{ form_row(form.rooms)}}
                    </div>
                    <div class="alert alert-light">
                        <h2>Détails de l'annonce</h2>
                        <hr>
                        {{ form_row(form.introduction)}}
                        {{ form_row(form.content)}}
                    </div>
                </div>
                <div class="col">
                    <div class="alert alert-light">
                        <h2>Image de l'annonce</h2>
                        <hr>
                        {{ form_row(form.coverImage)}}
                        {{ form_row(form.images)}}
                    </div>
                </div>
            </div>
            <div class="alert alert-success clearfix">
                <h2 class="alert-heading">Sauvegarder mon annonce</h2>
                <p>Vous êtes sur le point de modifier votre annonce.</p>
                <button type="submit" class="btn btn-primary float-right">
                    <i class="fas fa-check"></i>
                    Eneregistrer les modifications
                </button>
            </div>
        {{ form_end(form)}}
    </div>
{% endblock %}

{% block javascripts %}
    <script src="/build/runtime.9662464f.js"></script>
    <script src="/build/1.5fa25c9b.js"></script>
    <script src="/build/js/app.45302a72.js"></script>
    <script src="/build/0.d34258a3.js"></script>
    <script src="/build/js/ad.e76b6dac.js"></script>
{% endblock %}